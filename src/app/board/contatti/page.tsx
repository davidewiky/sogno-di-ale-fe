import { Box } from "@mui/material";

export default function ContattiPage() {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "20px",
      }}
    >
      CONTATTI
    </Box>
  );
}
