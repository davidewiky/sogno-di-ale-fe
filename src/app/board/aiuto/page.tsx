import { Box } from "@mui/material";

export default function AiutoPage() {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "20px",
      }}
    >
      AIUTO
    </Box>
  );
}
