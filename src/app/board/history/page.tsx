import { Box } from "@mui/material";

export default function HistoryPage() {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "20px",
      }}
    >
      LA NOSTRA STORIA
    </Box>
  );
}
