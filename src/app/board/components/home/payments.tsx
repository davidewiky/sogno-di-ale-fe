import { Typography } from "@mui/material";
import { SectionBox } from "~/app/board/components/home/section-box";

export function Payments() {
  return (
    <SectionBox>
      <Typography variant="h4">Aiutaci con la ricerca</Typography>
      <Typography variant="body1">Paga</Typography>
    </SectionBox>
  );
}
