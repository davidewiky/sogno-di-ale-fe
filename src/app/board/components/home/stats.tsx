import { Typography } from "@mui/material";
import { SectionBox } from "~/app/board/components/home/section-box";

export function HomeStats() {
  return (
    <SectionBox>
      <Typography variant="h4">Statistiche</Typography>
      <Typography variant="body1">Visualizza statistiche</Typography>
    </SectionBox>
  );
}
