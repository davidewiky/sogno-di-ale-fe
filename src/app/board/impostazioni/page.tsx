import { Box } from "@mui/material";

export default function ImpostazioniPage() {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "20px",
      }}
    >
      IMPOSTAZIONI
    </Box>
  );
}
